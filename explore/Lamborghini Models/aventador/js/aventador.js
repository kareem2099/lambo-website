// Add any interactive behavior for the Aventador page here

document.addEventListener("DOMContentLoaded", () => {
    const video = document.querySelector("video");

    // Automatically play video with a message
    if (video) {
        console.log("Enjoy the Lamborghini Aventador experience!");
    }
});
